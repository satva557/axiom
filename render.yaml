services:
  - type: web
    name: axiom-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend.py
    autoDeploy: true
envVars:
  - key: PORT
    value: 10000

